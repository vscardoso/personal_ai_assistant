# Criar requirements.txt minimal sem conflitos
echo 'fastapi
uvicorn[standard]
python-multipart
sqlalchemy
alembic
openai
pydantic
pydantic-settings
python-dotenv
httpx
jinja2' > requirements.txt

# Fazer commit
git add requirements.txt
git commit -m "Minimal requirements without version conflicts"
git push origin main